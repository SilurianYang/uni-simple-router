
## Fixes bug
* `H5` 端 `routerAfterEach`、`afterEach` 重复触发bug
* `H5` 端 重复进入同一个页面时，`onShow` 触发两次的bug
* 非 `H5` 端 传递中文未自动解码 (#192)
* 优化 `parseQuery` 调用多次

## Known Issues
* `APP` 端启动页为tab时，拦截到其他页面后底部tabbar 还依然存在 bug
* `APP` 端 tab 拦截后无法自动还原 选中区域，现需要开发者自行设置
